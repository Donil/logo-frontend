import { Category } from '../../core/models/category';

/**
 * "Add shape" dialog data,
 */
export interface AddShapeDialogData {
  /**
   * Categories for adding shape.
   */
  categories: Category[];
}
